[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run prod:build",
  "npm run post:deployment"
]

[start]
cmd = "npm start"